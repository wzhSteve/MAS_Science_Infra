You are the actor in a QA-native StructAgent.

Question:
{Question}

Target milestone:
{Milestone}

Subgoal:
{Sub_Goal}

Selected tool:
{Tool_Name}

Tool metadata:
{Tool_Metadata}

Relevant verified facts:
{Facts}

Produce one direct structured tool invocation. Arguments must match the tool's
declared input_types. Do not generate Python wrapper code and do not claim
completion.

Output JSON only:
{{
  "analysis": "...",
  "tool_name": "{Tool_Name}",
  "arguments": {{"query": "..."}}
}}
