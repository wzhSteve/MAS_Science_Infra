# Plan

本阶段把第一阶段已经可信的执行位置做成可编排 UI，而不是把旧 Rollout Sampling 面板照搬回来。用户能在画布和简化路径上找到同一站点，理解其触发条件、预算与实际可执行状态，保存后 Preflight 显示同一组有效值。

依赖[第一阶段：可执行站点与续跑前缀](采样编排第一阶段-可执行站点与续跑前缀.md)。训练仍由 `mas/train_tir_agent.py` 和 `rl/hooks/` 执行；前端不推导训练计划或重复实现 Gate。

## Position in the Project

`workflow.sampling` 是唯一可编辑数据源；Workflow 主画布表达 MAS 拓扑，简化路径表达“可选择的执行窗口”，Inspector 编辑被选中的 Site。路径不是第二张 Workflow，也不是单题调试的实际轨迹；训练结果仍由后续 Rollout Tree 展示。

```text
Workflow 草稿
   ├─ 主画布：Agent / Tool / 路由 + 站点角标
   └─ 采样路径：候选窗口 → Site Inspector
                         ↓ 同一份 sampling.sites
                   保存 → 后端有效站点解析 → Preflight
```

## Requirements

- UI 的候选、可执行状态、站点计数与第一阶段的服务端有效站点规则一致；不以 `effectiveSites()` 在浏览器里伪造已启用 Site。
- 画布选择与路径视图选择指向相同 Site 身份。一个按钮只修改 `workflow.sampling`，不复制到另一份前端状态。
- 分清“配置了站点”“训练可能到达边界”“该 run 真正命中/分支”三个状态；设计时的角标不能宣称已有训练结果。
- 只允许启用当前有事件和续跑前缀的站点及有信号的 Gate；不支持的已有 YAML 保留、可查看和修复，不在无关编辑时丢字段。
- `group_n` 是每题最终候选数，`initial_rollouts` 是先跑的独立轨迹数，`beam_size` 是每个站点/快照的上限；局部覆盖为空才继承全局。不能把这三者当成解码 beam。
- 用户切换 Sampling mode、推荐配置或保存 RL 设置时，Workflow Sampling 的最终算法与候选数优先级保持现状；Preflight 显示覆盖来源。
- 没有自动 GPU 请求；单题 Mock 与 Collect 的独立轨迹不得标作训练分支验收。

## Scope

- In：站点选择、可执行状态、Gate/次数/局部预算编辑、候选预览、无损保存与启动摘要。
- In：兼容旧 barrier 和原有 Site 的高级字段、未知扩展字段。
- Out：新建完整第二套 Workflow 编辑器、由 UI 计算 entropy/RAE、伪造 Router/边/token 分支能力。
- Out：展示运行后的树节点和实时奖励；属于后续树阶段。

## Current State

- `SamplingSettings.tsx` 是长列表加字段表单；`branchSites.ts` 从画布结构生成 Agent、Tool、Verifier、Router、边等候选，并把旧 barrier 展开成逐实体角标，但运行时没有全部相应事件。
- `MasGraphEditor.tsx` 已有 branch 数量角标，`useMasDraft.ts` 会将完整 Workflow 保存到实验；`workflowGraph.ts` 的序列化以 `...base` 保留 Sampling/扩展字段。
- 当前 Inspector 可设置 `when/nth`、Gate、局部 beam 和 reward，但默认 Site 已写死 `fork.beam_size=2`，使“留空继承全局”只在用户清空后才成立。
- Preflight 确认框显示算法来源、候选数与站点计数；尚未展示每个站点是否真正可执行及其阻断原因。

## Proposed Architecture

### 三层 UI

1. 顶层策略条：Sampling mode、总候选数、初始 Rollout、全局分支宽度与深度，并用文字说明算法 preset 不等同于分支发生。
2. 简化执行路径：从已编译 Workflow 派生入口、Agent/Tool 窗口及受支持的连接边界；明确标识“可配置”“暂不支持”“旧数据待修复”。不把图上的每条边自动等价成可恢复窗口。
3. Site Inspector：按 `anchor` 稳定定位；展示实际事件类型、可恢复快照来源、Gate 所需信号、触发次数、局部宽度/继承和 Reward。画布角标与这里双向定位，均编辑同一个 Workflow 草稿。

已有但暂不支持的站点要显示身份和缺失原因，保留原始字段；未声明的 legacy 默认站点以“兼容规则”单列，不冒充已创建的逐 Tool Site。开启显式站点时才物化 Site；失效节点和未知扩展字段不应因编辑别的节点而丢失。

### 保存和有效预览

前端只维护草稿；保存后的站点摘要由服务端同一有效解析器返回或通过只读检查取得。草稿可做本地输入提示，但不得替代服务端对能力/参数的裁决。训练时仍使用保存 → Preflight → 确认 → 创建 run；运行快照保存最终 Sampling。

## Files and Entry Points

| 区域 | 调整职责 |
| --- | --- |
| `webui/src/features/sampling/model/branchSites.ts` | 区分候选、legacy 规则与显式 Site，保持稳定锚点和字段 |
| `webui/src/features/sampling/components/SamplingSettings.tsx` | 策略条、路径视图、Inspector 和能力反馈 |
| `webui/src/features/mas/components/MasGraphEditor.tsx` | 可执行/未支持站点角标和双向定位，不改变 Workflow 拓扑 |
| `webui/src/features/mas/model/useMasDraft.ts` | 单一草稿、无损保存及失败反馈 |
| `science_infra/control/training.py`、`webui/src/shared/ui/alert-dialog.tsx` | 将服务端有效 Site 数与原因显示在启动检查 |
| `webui/src/features/sampling/model/*.test.ts` | 旧/新 YAML、锚点、继承与无损序列化 |

## Action Items

- [ ] 将候选能力和显式/legacy Site 区分，并显示不可执行原因。
- [ ] 增加单源简化执行路径，主画布与 Inspector 双向定位。
- [ ] 收口 Gate、触发次数、全局/局部预算编辑及继承语义。
- [ ] 保留不可识别/暂不支持的原始 Site；不静默删除旧数据。
- [ ] 用服务端有效解析结果核对保存、Preflight 与快照。

## Lightweight Validation

用纯前端 fixture 验证旧 barrier、两个不同 Tool 的显式 Site、未知扩展字段、已删除节点、局部 beam 继承、开关和路由定位；使用现有无 GPU 配置检查及 WebUI 构建。真实执行只由用户在服务器 WebUI 显式触发。

## Acceptance and Delivery Boundary

- 画布与路径视图操作的是同一 Site；刷新后状态、Gate 参数、未知字段都未丢失。
- 默认 legacy `after_tool` 在 UI 与 Preflight 都算一个泛化站点；显式两个不同 Tool 才显示两个具名站点。
- 暂不支持的 Router/边/Verifier/token 不会显示为“可执行”，也不会在启动检查后被静默当成首次 Tool 分支。
- 用户能看懂最终候选数、初始轨迹、分支上限和算法来源；本阶段不把画布预览宣称为训练结果。

## Risks and Edge Cases

- 路径视图是静态配置预览，动态 Router 走向不能由画布连线提前保证。
- 删除节点后仍保存的旧 Site 需要可见且可修复，不能因重新序列化丢失。
- 本阶段不实现 `on_token`；当前设计将它列为后续窗口内能力，旧配置仅展示其不支持状态。

## Next Phase

[第三阶段：按 run 存储 Rollout Tree](RolloutTree第一阶段-按run存储与结果回填.md)。
